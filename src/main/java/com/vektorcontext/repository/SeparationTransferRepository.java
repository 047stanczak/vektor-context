package com.vektorcontext.repository;

import com.vektorcontext.models.SeparationTransfer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SeparationTransferRepository extends JpaRepository<SeparationTransfer, Long> {
}
